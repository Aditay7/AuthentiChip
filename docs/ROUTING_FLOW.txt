┌─────────────────────────────────────────────────────────────────────┐
│                    ROLE-BASED ROUTING FLOW                          │
└─────────────────────────────────────────────────────────────────────┘

┌──────────────┐
│   FRONTEND   │
│  Login Page  │
└──────┬───────┘
       │
       │ POST /api/v1/auth/login
       │ { email, password }
       │
       ▼
┌──────────────────────┐
│      BACKEND         │
│  Validate Password   │
│  Generate JWT Token  │
└──────┬───────────────┘
       │
       │ Response:
       │ {
       │   "access_token": "eyJhbG...",
       │   "token_type": "bearer",
       │   "user": {
       │     "id": "123",
       │     "email": "user@example.com",
       │     "name": "John Doe",
       │     "role": "admin",  ← KEY FIELD!
       │     "organization": "Factory A"
       │   }
       │ }
       │
       ▼
┌──────────────────────┐
│     FRONTEND         │
│  Store Token & User  │
│  localStorage.set()  │
└──────┬───────────────┘
       │
       │ Check user.role
       │
       ├─────────────────┬─────────────────┐
       │                 │                 │
       ▼                 ▼                 ▼
  role="admin"     role="worker"    role=unknown
       │                 │                 │
       │                 │                 │
       ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│    ADMIN     │  │   WORKER     │  │    ERROR     │
│  DASHBOARD   │  │  DASHBOARD   │  │   HANDLER    │
└──────────────┘  └──────────────┘  └──────────────┘


═══════════════════════════════════════════════════════════════════════

                        PAGE REFRESH FLOW

┌──────────────────┐
│    FRONTEND      │
│  Page Loads      │
└────────┬─────────┘
         │
         │ Check localStorage
         │
         ├─────────────────┬──────────────────┐
         │                 │                  │
         ▼                 ▼                  ▼
    No Token         Has Token          Token Expired
         │                 │                  │
         │                 │                  │
         ▼                 │                  ▼
  ┌──────────┐            │            ┌──────────┐
  │  LOGIN   │            │            │  LOGIN   │
  │   PAGE   │            │            │   PAGE   │
  └──────────┘            │            └──────────┘
                          │
                          │ Option A: Use stored user.role
                          │ Option B: Verify with backend
                          │
                          │ GET /api/v1/auth/me
                          │ Authorization: Bearer <token>
                          │
                          ▼
                   ┌──────────────┐
                   │   BACKEND    │
                   │ Verify Token │
                   │ Return User  │
                   └──────┬───────┘
                          │
                          │ Response:
                          │ {
                          │   "id": "123",
                          │   "email": "...",
                          │   "role": "admin",
                          │   ...
                          │ }
                          │
                          ▼
                   ┌──────────────┐
                   │  FRONTEND    │
                   │ Check role   │
                   └──────┬───────┘
                          │
                          ├─────────────┬─────────────┐
                          │             │             │
                          ▼             ▼             ▼
                     role="admin"  role="worker"  Invalid
                          │             │             │
                          ▼             ▼             ▼
                   ┌──────────┐  ┌──────────┐  ┌──────────┐
                   │  ADMIN   │  │  WORKER  │  │  LOGIN   │
                   │DASHBOARD │  │DASHBOARD │  │   PAGE   │
                   └──────────┘  └──────────┘  └──────────┘


═══════════════════════════════════════════════════════════════════════

                    ROUTE PROTECTION EXAMPLE

User tries to access: /admin/dashboard
         │
         ▼
┌──────────────────┐
│  Route Guard     │
│  Check Auth      │
└────────┬─────────┘
         │
         │ Is user logged in?
         │
         ├─────────────┬──────────────┐
         │             │              │
         ▼             ▼              ▼
        NO           YES            YES
         │        role="admin"   role="worker"
         │             │              │
         ▼             ▼              ▼
  ┌──────────┐  ┌──────────┐  ┌──────────────┐
  │  LOGIN   │  │  ALLOW   │  │   REDIRECT   │
  │   PAGE   │  │  ACCESS  │  │  to /worker  │
  └──────────┘  └──────────┘  └──────────────┘


═══════════════════════════════════════════════════════════════════════

                        SECURITY LAYERS

┌─────────────────────────────────────────────────────────────────────┐
│                         FRONTEND (UX Layer)                         │
│  • Check role from localStorage                                     │
│  • Redirect to appropriate dashboard                                │
│  • Hide/show UI elements based on role                              │
│  • NOT FOR SECURITY - Only for user experience                      │
└─────────────────────────────────────────────────────────────────────┘
                                 │
                                 │ API Request
                                 │ Authorization: Bearer <token>
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      BACKEND (Security Layer)                       │
│  • Verify JWT token signature                                       │
│  • Extract user ID from token                                       │
│  • Fetch user from database                                         │
│  • Check user.role for authorization                                │
│  • THIS IS THE REAL SECURITY                                        │
└─────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════

                    IMPLEMENTATION CHECKLIST

BACKEND (✅ DONE):
  ✅ Login returns user.role
  ✅ /auth/me returns complete user info
  ✅ JWT token contains user ID
  ✅ All protected endpoints verify token

FRONTEND (📝 YOUR TODO):
  ☐ Store token in localStorage after login
  ☐ Store user object in localStorage
  ☐ Read user.role from login response
  ☐ Implement redirect logic:
      if (role === 'admin') → /admin/dashboard
      if (role === 'worker') → /worker/dashboard
  ☐ On page load, check localStorage for token
  ☐ If token exists, verify with /auth/me
  ☐ Implement route guards for protected pages
  ☐ Handle 401 errors (token expired)
  ☐ Implement logout (clear localStorage)


═══════════════════════════════════════════════════════════════════════

                        QUICK REFERENCE

Login Response Structure:
{
  "access_token": "string",
  "token_type": "bearer",
  "user": {
    "id": "string",
    "email": "string",
    "name": "string | null",
    "role": "admin" | "worker",  ← Use this for routing!
    "organization": "string | null"
  }
}

Frontend Routing Logic:
const { user } = loginResponse;
if (user.role === 'admin') {
  navigate('/admin/dashboard');
} else if (user.role === 'worker') {
  navigate('/worker/dashboard');
}

Token Verification:
GET /api/v1/auth/me
Headers: { Authorization: "Bearer <token>" }
Returns: Complete user object with role
